# New custom-field lesson

Fill what is known; leave the rest marked unknown. The user can provide ordinary text instead of filling this template; the agent organizes the lesson.

- Lesson ID, revision and date:
- Field name:
- Purpose in everyday language:
- Example natural-language requests:
- Source (user statement, screenshot/export, document and page):
- Exact formula, copied without editing:
- Empower version/build:
- Field scope/type, data type, units, precision:
- Calculation and selection settings:
- Sample Type, Peak Type and Missing Peak:
- Translation table and Use As mode for Boolean/Enumerated fields:
- Search Order and All or Nothing for intersample fields:
- Result-set processing order and Summarize Custom Fields row placement:
- Dependencies and their calculation order:
- Each input's meaning, units, percent/fraction convention and default:
- Standard/sample preparation details:
- Labels, injections, channels and components if relevant:
- Built-in Dilution/SampleWeight and calibration conditions if relevant:
- When this field applies, and known limitations:
- Complete example inputs:
- Expected result and how established:
- Observed Empower result, configuration, date and evidence:
- Missing/zero-input behavior actually observed:
- Status of each key claim (user-provided/user-confirmed/assistant-inferred/arithmetic-checked/Empower-tested):
- Open questions:
- Correction history (date, previous value, new value, reason and source):

Keep worked arithmetic distinct from observed Empower output. Record rounding/display differences rather than changing expected values to hide discrepancies.
